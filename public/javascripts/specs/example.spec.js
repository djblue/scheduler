define([/* define dependencies */], function () {

    describe("An example", function() {
    
        // executed before each
        beforeEach(function () {});
        // expect after each
        afterEach(function () {});

        // an expected behavior
        it("should be true", function() {
            expect(true).toBe(true);
        });

    });

});
